# HTML 速查卡(第 1 课)

## 页面骨架(每个页面都有)

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <title>标签页标题</title>
</head>
<body>
  <!-- 页面可见内容都写在这里 -->
</body>
</html>
```

## 常用标签

| 标签 | 作用 |
|------|------|
| `<h1>` ~ `<h6>` | 标题,数字越小字越大 |
| `<p>` | 段落 |
| `<strong>` | 加粗强调 |
| `<ul>` + `<li>` | 无序列表(圆点) |
| `<ol>` + `<li>` | 有序列表(数字) |
| `<a href="网址">文字</a>` | 链接 |
| `<!-- 内容 -->` | 注释,浏览器不显示 |
| `<img src="图片地址" alt="描述">` | 图片(第 2 课会用) |

## 常用属性

| 属性 | 写在哪 | 作用 |
|------|--------|------|
| `href` | `<a>` | 跳转地址 |
| `target="_blank"` | `<a>` | 新窗口打开 |
| `src` | `<img>` | 图片来源 |
| `alt` | `<img>` | 图片加载失败时显示的文字 |

## 新手常见错误

1. 标签没闭合:`<p>文字` ← 少了 `</p>`
2. 中文乱码 ← 忘了 `<meta charset="UTF-8">`
3. 属性没加引号:`<a href=baidu.com>` ← 应写 `href="https://baidu.com"`
4. 内容写错位置:把该显示的内容写进了 `<head>` 里
