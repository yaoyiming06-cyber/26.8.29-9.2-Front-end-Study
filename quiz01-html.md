# 第 1 课自查测验(HTML)

先不看任何资料,默答下面 6 题。答不出说明还没"学会",回去再看一遍速查卡。

1. `<h1>` 和 `<h6>` 谁的默认字号更大?
2. 链接用哪个标签?跳转地址写在哪个属性里?
3. 想让链接在新窗口打开,要加什么属性?
4. 无序列表(圆点列表)由哪两个标签组合而成?
5. 浏览器里看不到、只写给开发者看的东西叫什么?
6. 页面上用户能看到的所有内容,必须写在哪个标签内部?

---

## 参考答案

1. `<h1>`(数字越小字越大)
2. `<a>` 标签,地址写在 `href` 属性里
3. `target="_blank"`
4. `<ul>`(外层)+ `<li>`(每一项)
5. 注释:`<!-- -->`
6. `<body>`
