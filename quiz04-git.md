# 第 4 课自查测验(Git,7 题)

先默答,答不出回去看主页第 4 课讲义 / `cheatsheet-git.md`。

1. `git init` 是干什么的?一个项目需要执行几次?
2. 修改完代码想"存档",要按顺序敲哪两个命令?
3. `git commit -m "备注"` 里的备注应该怎么写?举例。
4. `git status` 能告诉你什么?
5. 第一次把本地仓库推送到 GitHub,用哪条命令?为什么带 `-u`?
6. 之后每次推送,只需要敲什么?
7. 说出"工作区 / 暂存区 / 本地仓库 / 远程仓库"各自的角色(一句话一个)。

---

## 参考答案

1. 初始化 Git 仓库(开始记录版本),一个项目一次
2. `git add .`(放进暂存区)→ `git commit -m "备注"`(存档)
3. 写清"做了什么":如 `git commit -m "新增:个人介绍页标题"`;别写"更新""123"
4. 当前状态:改了哪些文件、哪些还没 add / 还没 commit
5. `git push -u origin main`;`-u` 记住远程仓库,以后直接 `git push`
6. `git push`
7. 工作区=正在改的文件;暂存区=准备提交的改动(购物车);本地仓库=历史存档;远程仓库=GitHub 上的云端备份
