# 新手操作指南(从 0 开始)

## 1. 用 VS Code 打开学习文件夹

1. 打开 VS Code
2. 菜单栏 **File → Open Folder…**
3. 选择文件夹:`Documents/deepseek harness/学习/stage0`
4. 左侧边栏就能看到所有文件了

## 2. 新建你的作业文件

1. 左侧文件列表区域,点"新建文件"图标(一张纸带 + 号的图标)
2. 输入文件名:`lesson01.html`(一定要带 `.html` 后缀)
3. 回车,开始写代码

## 3. 保存与预览

- **保存**:`Cmd + S`(每次改完都要存)
- **预览**:回到 Finder(访达),双击 `lesson01.html`,就会用默认浏览器打开
- 改完代码 → 保存 → **回到浏览器按 `Cmd + R` 刷新**,就能看到最新效果

## 4. 跑作业验收脚本

1. VS Code 菜单栏 **Terminal → New Terminal**(打开终端)
2. 确认当前目录是 stage0(终端显示路径以 `stage0` 结尾即可)
3. 输入并回车:
   ```
   node check-lesson01.mjs lesson01.html
   ```
4. 看到 14 项全 ✅ 就说明作业合格了

## 5. 卡住了怎么办?

- 直接把**现在的代码 + 遇到的问题**贴到对话里,哪怕只写了一半也行
- 报错信息看不懂?原样复制给我
- 不要因为卡住就停下——发出来就是进步
