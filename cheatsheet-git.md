# Git 速查卡(第 4 课)

## Git 是什么

**版本管理工具**:记录你每次改了什么,可以随时回到任意一个历史版本。就像游戏存档。

## 三个区域(核心概念)

```
工作区(你正在改的文件)
   │  git add          ← 放进"购物车"
   ▼
暂存区(准备提交的改动)
   │  git commit       ← 结账,存档,写备注
   ▼
本地仓库(历史存档)
   │  git push         ← 上传到云端
   ▼
远程仓库(GitHub)
```

## 常用命令(按顺序记这 7 条就够)

| 命令 | 作用 |
|------|------|
| `git init` | 在项目文件夹里初始化仓库(只做一次) |
| `git status` | 查看当前状态:改了什么、哪些没提交 |
| `git add .` | 把当前目录所有改动加入暂存区 |
| `git commit -m "备注"` | 存档,备注写清楚改了什么 |
| `git remote add origin 仓库地址` | 关联 GitHub 仓库(只做一次) |
| `git push -u origin main` | 推送到 GitHub(第一次) |
| `git push` | 之后每次推送只要这个 |

## 一个常见工作循环

```bash
改代码 → git add . → git commit -m "完成个人介绍页" → git push
```

## 新手常见错误

1. 没 `cd` 到项目目录就敲命令(先用 `pwd` 看看自己在哪)
2. commit 备注乱写(写"更新""123"没人看得懂,写"新增:个人介绍页标题")
3. 忘记 `git add .` 直接 commit(会提示"没有改动可提交")
4. 首次 push 被拒:没先关联 GitHub 仓库(`git remote add`)
